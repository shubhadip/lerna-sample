{
  "name": "root",
  "private": true,
  "workspaces":["packages/*"],
  "version": "1.0.0",
  "scripts": {
    "test": "lerna run test",
    "diff": "lerna diff",
    "new_version":"lerna version --conventional-commits --yes"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
